on: [push]

jobs:
  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-go@v2
        with:
          go-version: 1.15

      - run: go test -cover ./...
        env:
          CGO_ENABLED: 0

      - run: go run ./cmd/goreview ./...
